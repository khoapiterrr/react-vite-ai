import type { Meta } from '@/types/routes';
import PageTemplate from '@/components/PageTemplate';

const Profile = (props: Meta) => {
  return (
    <PageTemplate meta={props}>
      <div>
        {/* Add your profile content here */}
      </div>
    </PageTemplate>
  );
};

export default Profile; 